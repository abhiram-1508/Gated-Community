# Public assets

Static frontend files served directly by Vite.

- `favicon.svg` is the app icon.
- `manifest.json` enables installable/PWA-style metadata.
- Add `logo.png` here if a raster college/project logo is required for submission.
